package xmr

import (
	"errors"
)

// SendTransaction send signed tx
func (b *Bridge) SendTransaction(signedTx interface{}) (txHash string, err error) {
	return "", errors.New("Not impelemnted yet")
}
